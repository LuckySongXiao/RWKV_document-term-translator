<Window x:Class="DocumentTranslator.Windows.TranslationRulesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="翻译规则编辑器" Height="520" Width="720"
        WindowStartupLocation="CenterOwner"
        MinHeight="400" MinWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🧩 翻译规则编辑器" FontSize="16" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="StatusText" Text="（就绪）" Margin="8,0,0,0" Foreground="LightGray"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Header="敏感提示词清单（命中即清除）" Grid.ColumnSpan="2" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="说明：以下列表中的任意词/句在译文中出现将被清洗去除。支持正则（每行一条）。" Foreground="Gray" FontSize="12"/>
                    <DockPanel Margin="0,6,0,0">
                        <TextBox x:Name="NewRuleBox" DockPanel.Dock="Left" Width="420" Height="28" Margin="0,0,6,0"/>
                        <Button Content="➕ 添加" Width="80" Click="AddRule"/>
                        <Button Content="🗑️ 删除选中" Width="100" Margin="6,0,0,0" Click="RemoveSelectedRule"/>
                    </DockPanel>
                    <ListBox x:Name="RulesList" Height="150"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="示例（命中将被清除）" Grid.Row="1" Grid.Column="0" Margin="0,0,6,0">
                <TextBox x:Name="ExamplesBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                         Text="请提供纯正的中文翻译\nPlease provide a pure English translation\nXin cung cấp bản dịch tiếng Việt thuần túy:"/>
            </GroupBox>

            <GroupBox Header="清洗预览" Grid.Row="1" Grid.Column="1" Margin="6,0,0,0">
                <DockPanel>
                    <Button Content="🔍 预览清洗" DockPanel.Dock="Top" Height="28" Margin="0,0,0,6" Click="PreviewClean"/>
                    <TextBox x:Name="PreviewOutput" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
                </DockPanel>
            </GroupBox>
        </Grid>

        <DockPanel Grid.Row="2" Margin="10,0,10,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 保存" Width="90" Margin="6,0,0,0" Click="SaveRules"/>
                <Button Content="关闭" Width="90" Margin="6,0,0,0" Click="CloseWindow"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>

